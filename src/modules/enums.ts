export enum Urls {
    TokenRequest = "https://api.blip.ai/accounts/{login}/tokens"
}

export enum StoreMethod {
    Inject = "inject",
    Set = "set",
    Get = "get"
} 

export enum Pages {
    Login = "login.html",
    Bots = "bots.html"
}